/**
 * Buyer Layout
 * Forces desktop viewport for consistent layout across all devices
 */

export const viewport = {
  width: 1024,
  themeColor: '#3B82F6',
}

export default function BuyerLayout({ children }) {
  return <>{children}</>
}
